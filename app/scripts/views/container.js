/*global define*/

define([
    'jquery',
    'underscore',
    'backbone',
    'backbone.viewmanager',
    'views/header'
], function ($, _, Backbone, BBViewManager, HeaderView) {
    'use strict';

    var ContainerView = Backbone.View.extend({

    });

    return ContainerView;
});
